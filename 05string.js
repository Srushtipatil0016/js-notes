const name = "hitesh"
const repoCount = 50
//console.log(name + repoCount + "Value");     ----- outdated to add stings 

// so use backtakes ``  -- in that we create  placeholdeer (to inject variable) by ${}


//console.log(`hello my name ss ${name} and my repo count is ${repoCount}` );

const gameName = new String('hitesh-hc')
console.log(gameName.charAt(2))
